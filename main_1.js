let a = 12  // In let variable we can change the value
let b = "ram" 

const c = "some constant value" // we cannot change the value cause it is constant variable

// true => true, "dhfjkash", if not 0, {}, [], 1
// false => false, 0, "",  null, undefined, NaN
let isloggedIn = true

const name = myName || "ram"

const isOld = false
const age = isOld && 70

if (4>1) {
    // statement
    console.log("4 is greater than 1")  
    
} else {
    // statement
    console.log("4 is not greater than 1")
    
}

// Function 
function hello(x){
    console.log("Hello",x)
}

function add(a, b) {
    return a + b;
    }


    // Array 
    const colors = ["red", "green", "blue"] //it should be in list
    const item = colors[0]
    console.log(item) // red is output


console.log ("Hello World")

// turnery operator
const name1 = 2>1 ? "ram" : "shyam"

